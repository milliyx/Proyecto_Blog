const toggleSwitch = document.getElementById("private");

toggleSwitch.addEventListener("change", function () {
  if (this.checked) {
    // Si está marcado
    // Aquí puedes realizar acciones adicionales cuando se activa el interruptor
  } else {
    // Si no está marcado
    // Aquí puedes realizar acciones adicionales cuando se desactiva el interruptor
  }
});
